import { Component } from '@angular/core';
import { Facebook } from "@ionic-native/facebook/ngx";
import { Parse } from 'parse';

@Component({
  selector: 'app-home',
  templateUrl: 'home.page.html',
  styleUrls: ['home.page.scss'],
})
export class HomePage {

  constructor() {

  }

  onOpenPage(option: number) {
    
  }

}
